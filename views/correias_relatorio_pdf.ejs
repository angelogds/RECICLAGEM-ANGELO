<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8">
<title>Relatório de Correias</title>

<style>
    body {
        font-family: Arial, sans-serif;
        font-size: 12px;
        margin: 20px;
        color: #222;
    }

    h1 {
        text-align: center;
        color: #0D6B33;
        margin-bottom: 6px;
        font-size: 18px;
    }

    .sub {
        text-align: center;
        font-size: 11px;
        color: #555;
        margin-bottom: 16px;
    }

    table {
        width: 100%;
        border-collapse: collapse;
        font-size: 11px;
        margin-top: 10px;
    }

    th {
        background: #0D6B33;
        color: #fff;
        padding: 8px;
        border: 1px solid #ccc;
        font-weight: 700;
    }

    td {
        padding: 7px;
        border: 1px solid #ddd;
        vertical-align: top;
    }

    .center {
        text-align: center;
    }

    .footer {
        margin-top: 18px;
        text-align: center;
        font-size: 10px;
        color: #777;
    }

</style>
</head>

<body>

<!-- Logo -->
<% if (logo) { %>
<div style="text-align:center; margin-bottom:10px;">
    <img src="<%= logo %>" style="width:80px;">
</div>
<% } %>

<h1>Relatório de Correias</h1>
<div class="sub">
    Gerado em <%= dataGeracao %> — Total: <%= correias.length %>
</div>

<table>
    <thead>
        <tr>
            <th style="width:50px;">ID</th>
            <th>Nome</th>
            <th>Modelo</th>
            <th>Medida</th>
            <th style="width:80px;">Quantidade</th>
        </tr>
    </thead>

    <tbody>

        <% if (correias.length === 0) { %>
            <tr>
                <td colspan="5" class="center">Nenhuma correia cadastrada.</td>
            </tr>
        <% } %>

        <% correias.forEach(c => { %>
        <tr>
            <td class="center"><%= c.id %></td>
            <td><%= c.nome %></td>
            <td><%= c.modelo %></td>
            <td><%= c.medida %></td>
            <td class="center"><%= c.quantidade %></td>
        </tr>
        <% }) %>

    </tbody>
</table>

<div class="footer">
    Campo do Gado — Sistema de Manutenção
</div>

</body>
</html>
