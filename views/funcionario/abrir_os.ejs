<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Abrir Ordem de Serviço</title>
  <link rel="stylesheet" href="/public/style.css">
</head>
<body>
  <% include ../partials/cabecalho.ejs %>

  <main class="container">
    <h1>🛠️ Abrir Ordem de Serviço</h1>

    <% if (!equip) { %>
      <p>Equipamento não encontrado.</p>
    <% } else { %>
      <section class="equip-info">
        <h2>Equipamento Selecionado</h2>
        <p><strong>ID:</strong> <%= equip.id %></p>
        <p><strong>Nome:</strong> <%= equip.nome %></p>
        <p><strong>Setor:</strong> <%= equip.setor %></p>
        <p><strong>Correias Utilizadas:</strong> <%= equip.correias_utilizadas %></p>

        <% if (equip.foto_path) { %>
          <img src="/<%= equip.foto_path %>" alt="Foto do equipamento" width="150">
        <% } %>
      </section>

      <section class="abrir-os-form">
        <h2>Registrar Ordem de Serviço</h2>
        <form action="/funcionario/abrir_os" method="POST" enctype="multipart/form-data" class="formulario">
          <input type="hidden" name="equipamento_id" value="<%= equip.id %>">

          <div class="form-group">
            <label for="descricao">Descrição do problema:</label>
            <textarea id="descricao" name="descricao" rows="4" required></textarea>
          </div>

          <div class="form-group">
            <label for="foto_antes">Foto antes da manutenção:</label>
            <input type="file" id="foto_antes" name="foto_antes" accept="image/*">
          </div>

          <button type="submit" class="btn">Abrir OS</button>
        </form>
      </section>
    <% } %>
  </main>

  <% include ../partials/rodape.ejs %>
</body>
</html>
