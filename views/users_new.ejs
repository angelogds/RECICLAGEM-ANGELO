<%- include("./partials/cabecalho") %>

<h1 class="page-title">
    <i class="fa-solid fa-user-plus"></i> Novo Usuário
</h1>

<div class="card form-card">

    <% if (error) { %>
        <div class="alert-error"><%= error %></div>
    <% } %>

    <form method="POST" action="/users/new" class="formulario">

        <div class="grupo">
            <label>Usuário (login / e-mail)</label>
            <input type="text" name="usuario" required>
        </div>

        <div class="grupo">
            <label>Senha</label>
            <input type="password" name="senha" required>
        </div>

        <div class="grupo">
            <label>Nome completo</label>
            <input type="text" name="nome">
        </div>

        <div class="grupo">
            <label>Papel</label>
            <select name="role" required>
                <option value="admin">Administrador</option>
                <option value="funcionario" selected>Funcionário</option>
                <option value="operador">Operador</option>
            </select>
        </div>

        <button class="btn btn-verde-claro btn-full">
            <i class="fa-solid fa-save"></i> Salvar Usuário
        </button>

    </form>

</div>

<%- include("./partials/rodape") %>
