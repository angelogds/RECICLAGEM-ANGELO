<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>Recuperar Senha</title>

    <link rel="stylesheet" href="/css/style.css">

    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: "Inter", sans-serif;
            background: url('/img/login-bg.png') no-repeat center center fixed;
            background-size: cover;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .forgot-box {
            width: 380px;
            padding: 32px;
            background: rgba(255,255,255,0.92);
            border-radius: 16px;
            box-shadow: 0 12px 28px rgba(0,0,0,0.25);
            backdrop-filter: blur(4px);
        }

        .forgot-title {
            font-size: 26px;
            font-weight: 800;
            text-align: center;
            color: var(--green-900);
            margin-bottom: 6px;
        }

        .forgot-sub {
            text-align: center;
            font-size: 14px;
            color: #355b41;
            margin-bottom: 18px;
        }

        .forgot-box input {
            padding: 12px;
            margin-top: 6px;
            margin-bottom: 16px;
            width: 100%;
            border: 1px solid #c8e0d2;
            border-radius: 10px;
            background: #f9fff9;
        }

        .btn-forgot {
            width: 100%;
            padding: 12px;
            border: none;
            background: var(--green-900);
            color: white;
            font-size: 16px;
            border-radius: 10px;
            font-weight: bold;
            cursor: pointer;
        }

        .btn-forgot:hover {
            background: var(--green-800);
        }

        .forgot-back {
            display: block;
            text-align: center;
            margin-top: 14px;
            font-size: 14px;
            font-weight: 600;
            color: var(--green-900);
        }
    </style>
</head>

<body>

<div class="forgot-box">

    <h1 class="forgot-title">Recuperar Senha</h1>
    <div class="forgot-sub">Digite seu e-mail para receber o link</div>

    <% if (error) { %>
        <div class="alert-error"><%= error %></div>
    <% } %>

    <form action="/forgot" method="POST">

        <label>E-mail</label>
        <input type="email" name="email" required>

        <button type="submit" class="btn-forgot">
            Enviar Link de Recuperação
        </button>

    </form>

    <a href="/login" class="forgot-back">← Voltar ao login</a>

</div>

</body>
</html>
