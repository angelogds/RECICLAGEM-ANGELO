<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" href="/public/style.css">
  </head>
<body>
  <%- include("../partials/cabecalho") %>

  <div class="box">
    <h2>Equipamentos</h2>

    <a href="/admin/equipamentos/novo">
      <button style="margin-bottom: 15px;">Cadastrar novo equipamento</button>
    </a>

    <ul>
      <% equipamentos.forEach(e => { %>
        <li>
          <b><%= e.nome %></b> — <%= e.setor %>

          <% if (e.qr_code) { %>
            | <a href="/<%= e.qr_code %>" target="_blank">Ver QR Code</a>
          <% } %>

          | <a href="/funcionario/abrir_os?equip_id=<%= e.id %>">Abrir OS</a>
        </li>
      <% }) %>
    </ul>
  </div>

</body>
</html>
