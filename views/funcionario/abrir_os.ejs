<h1 class="page-title">
    <i class="fa-solid fa-file-circle-plus"></i>
    Abrir Nova Ordem de Serviço
</h1>

<div class="card form-card">

    <form method="POST" action="/ordens" enctype="multipart/form-data" class="formulario">

        <div class="grupo">
            <label>Equipamento</label>
            <select name="equipamento_id" required>
                <option value="">Selecione...</option>
                <% equipamentos.forEach(eq => { %>
                    <option value="<%= eq.id %>"><%= eq.nome %></option>
                <% }) %>
            </select>
        </div>

        <div class="grupo">
            <label>Tipo</label>
            <input type="text" name="tipo" required placeholder="Mecânica / Elétrica / Preventiva / Corretiva">
        </div>

        <div class="grupo">
            <label>Descrição</label>
            <textarea name="descricao" rows="4" required></textarea>
        </div>

        <div class="grupo">
            <label>Fotos de Abertura (opcional)</label>
            <!-- 🔥 CORRETO PARA MULTER -->
            <input type="file" name="fotos" accept="image/*" multiple>
        </div>

        <button class="btn btn-verde-claro btn-full">
            <i class="fa-solid fa-floppy-disk"></i> Abrir OS
        </button>

    </form>

</div>

<a href="/ordens" class="btn btn-cinza-escuro">
    ← Voltar
</a>

<%- include("./partials/rodape") %>
